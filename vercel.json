{
  "buildCommand": "cd deriv-trader && npm install && npm run build",
  "outputDirectory": "deriv-trader/dist",
  "framework": null,
  "rewrites": [{ "source": "/:path*", "destination": "/index.html" }]
}
